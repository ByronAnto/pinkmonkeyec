services:
  app:
    restart: always
    environment:
      APP_ENV: production
      APP_DEBUG: "false"
  web:
    restart: always
    ports:
      - "127.0.0.1:8080:80"
  db:
    restart: always
  redis:
    restart: always
